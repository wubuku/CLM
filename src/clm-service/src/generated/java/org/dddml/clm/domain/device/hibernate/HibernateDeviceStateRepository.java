// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.clm.domain.device.hibernate;

import java.util.*;
import java.util.Date;
import org.dddml.clm.domain.*;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import org.dddml.clm.domain.device.*;
import org.dddml.clm.specialization.*;
import org.dddml.clm.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateDeviceStateRepository implements DeviceStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("DeviceId", "CabinetId", "Description", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public DeviceState get(String id, boolean nullAllowed) {
        DeviceState.SqlDeviceState state = (DeviceState.SqlDeviceState)getCurrentSession().get(AbstractDeviceState.SimpleDeviceState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractDeviceState.SimpleDeviceState();
            state.setDeviceId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (DeviceState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{DeviceState.SqlDeviceState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(DeviceState state) {
        DeviceState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (DeviceState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if(s.getVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(DeviceState detached) {
        DeviceState persistent = getCurrentSession().get(AbstractDeviceState.SimpleDeviceState.class, detached.getDeviceId());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().merge(detached);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(DeviceState persistent, DeviceState detached) {
        ((DeviceState.MutableDeviceState) detached).setVersion(persistent.getVersion());
    }

}

