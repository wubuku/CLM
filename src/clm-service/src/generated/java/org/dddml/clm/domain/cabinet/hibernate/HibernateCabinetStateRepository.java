// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.clm.domain.cabinet.hibernate;

import java.util.*;
import java.time.OffsetDateTime;
import org.dddml.clm.domain.*;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import org.dddml.clm.domain.cabinet.*;
import org.dddml.clm.specialization.*;
import org.dddml.clm.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateCabinetStateRepository implements CabinetStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("CabinetId", "Description", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public CabinetState get(String id, boolean nullAllowed) {
        CabinetState.SqlCabinetState state = (CabinetState.SqlCabinetState)getCurrentSession().get(AbstractCabinetState.SimpleCabinetState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractCabinetState.SimpleCabinetState();
            state.setCabinetId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (CabinetState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{CabinetState.SqlCabinetState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(CabinetState state) {
        CabinetState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (CabinetState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if(s.getVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(CabinetState detached) {
        CabinetState persistent = getCurrentSession().get(AbstractCabinetState.SimpleCabinetState.class, detached.getCabinetId());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().save(persistent);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(CabinetState persistent, CabinetState detached) {
        ((AbstractCabinetState) persistent).merge(detached);
    }

}

