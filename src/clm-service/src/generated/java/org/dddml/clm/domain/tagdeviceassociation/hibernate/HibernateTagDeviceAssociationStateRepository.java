// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.clm.domain.tagdeviceassociation.hibernate;

import java.util.*;
import org.dddml.clm.domain.*;
import java.util.Date;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import org.dddml.clm.domain.tagdeviceassociation.*;
import org.dddml.clm.specialization.*;
import org.dddml.clm.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateTagDeviceAssociationStateRepository implements TagDeviceAssociationStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("TagDeviceAssociationId", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public TagDeviceAssociationState get(TagDeviceAssociationId id, boolean nullAllowed) {
        TagDeviceAssociationState.SqlTagDeviceAssociationState state = (TagDeviceAssociationState.SqlTagDeviceAssociationState)getCurrentSession().get(AbstractTagDeviceAssociationState.SimpleTagDeviceAssociationState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractTagDeviceAssociationState.SimpleTagDeviceAssociationState();
            state.setTagDeviceAssociationId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (TagDeviceAssociationState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{TagDeviceAssociationState.SqlTagDeviceAssociationState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(TagDeviceAssociationState state) {
        TagDeviceAssociationState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (TagDeviceAssociationState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if(s.getVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(TagDeviceAssociationState detached) {
        TagDeviceAssociationState persistent = getCurrentSession().get(AbstractTagDeviceAssociationState.SimpleTagDeviceAssociationState.class, detached.getTagDeviceAssociationId());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().merge(detached);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(TagDeviceAssociationState persistent, TagDeviceAssociationState detached) {
        ((TagDeviceAssociationState.MutableTagDeviceAssociationState) detached).setVersion(persistent.getVersion());
    }

}

