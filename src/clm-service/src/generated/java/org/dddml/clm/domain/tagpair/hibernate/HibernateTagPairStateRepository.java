// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.clm.domain.tagpair.hibernate;

import java.util.*;
import org.dddml.clm.domain.*;
import java.time.OffsetDateTime;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import org.dddml.clm.domain.tagpair.*;
import org.dddml.clm.specialization.*;
import org.dddml.clm.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateTagPairStateRepository implements TagPairStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("TagPairId", "Description", "Version", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public TagPairState get(TagIdPair id, boolean nullAllowed) {
        TagPairState.SqlTagPairState state = (TagPairState.SqlTagPairState)getCurrentSession().get(AbstractTagPairState.SimpleTagPairState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractTagPairState.SimpleTagPairState();
            state.setTagPairId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (TagPairState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{TagPairState.SqlTagPairState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(TagPairState state) {
        TagPairState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (TagPairState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if(s.getVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(TagPairState detached) {
        TagPairState persistent = getCurrentSession().get(AbstractTagPairState.SimpleTagPairState.class, detached.getTagPairId());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().save(persistent);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(TagPairState persistent, TagPairState detached) {
        ((AbstractTagPairState) persistent).merge(detached);
    }

}

